apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service-ctx1
  namespace: theater-msa
  labels:
    app: booking-service
    tier: backend
    cluster: ctx1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: booking-service
      cluster: ctx1
  template:
    metadata:
      labels:
        app: booking-service
        tier: backend
        cluster: ctx1
    spec:
      containers:
      - name: booking-service
        image: harbor.27.96.156.180.nip.io/theater-msa/booking-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8083
        envFrom:
        - configMapRef:
            name: theater-config
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1
            preference:
              matchExpressions:
              - key: cluster-name
                operator: In
                values:
                - ctx1
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service-ctx1
  namespace: theater-msa
  labels:
    app: booking-service
    tier: backend
    cluster: ctx1
spec:
  type: ClusterIP
  ports:
  - port: 8083
    targetPort: 8083
    protocol: TCP
  selector:
    app: booking-service
    cluster: ctx1